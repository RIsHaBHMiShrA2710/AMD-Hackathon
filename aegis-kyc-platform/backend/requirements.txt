# AegisKYC — Backend Dependencies
# Latest stable versions (June 2026)
# Install with: pip install -r requirements.txt
#
# NOTE: easyocr is listed per spec but NOT required for the text pipeline.
# It pulls ~2GB of PyTorch. Uncomment only if image OCR is needed.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
langgraph>=0.2.28
langchain>=0.3.0
langchain-openai>=0.2.0
httpx>=0.27.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.25.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
# easyocr>=1.7.1  # Uncomment for image-based document OCR support
